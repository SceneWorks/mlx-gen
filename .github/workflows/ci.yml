name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least-privilege GITHUB_TOKEN (F-047): the default job token is write-capable, which CI does not
# need. Scoping to read-only contents matches the fetch-only operations the steps perform.
permissions:
  contents: read

# Supersede in-flight runs on the same ref (the macOS + MLX build is expensive).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The backend-neutral contract layer (gen-core) and its conformance testkit have ZERO tensor
  # deps, so they build and test on Linux — fast, and the proof that the contract is genuinely
  # backend-independent (a candle provider will satisfy the same testkit). This is the "gen-core CI
  # (Linux, stub provider)" lane from epic 3720 / sc-4481: the testkit's in-crate stub exercises
  # every conformance check (incl. the deliberately-broken stubs that prove each check fires).
  contract:
    name: gen-core + testkit (Linux, no tensor backend)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - name: Install Rust (pinned by rust-toolchain.toml)
        run: |
          rustup show
          rustup component add rustfmt clippy
          rustc --version
      - name: Cache cargo
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
      - name: Format (contract crates)
        run: cargo fmt -p sceneworks-gen-core -p sceneworks-gen-core-testkit --check
      - name: Clippy (contract crates)
        run: cargo clippy --locked -p sceneworks-gen-core -p sceneworks-gen-core-testkit --all-targets -- -D warnings
      - name: Test (contract crates + testkit stub conformance)
        run: cargo test --locked -p sceneworks-gen-core -p sceneworks-gen-core-testkit

  test:
    name: fmt + clippy + test (macOS / Metal GPU)
    # Apple-Silicon runner: MLX is Apple-Silicon-only and the tests eval on the
    # Metal GPU (the crate forces RUST_TEST_THREADS=1 via .cargo/config.toml,
    # since MLX's default Metal device is not thread-safe).
    #
    # macos-15 is the newest hosted Apple-Silicon image (no hosted macos-26 exists).
    # MLX 0.31.2's CPU SIMD does not compile against the SDK-14 headers on macos-14,
    # so we build here against SDK 15. The shipping config (.cargo/config.toml) targets
    # macOS 26 / Metal 400 for the NAX fast kernels; that needs SDK >= 26.2, which no
    # hosted runner provides, so CI lowers the deployment target to 15.0 (Metal 320) to
    # verify correctness. CI therefore does NOT exercise the NAX fast path — that
    # requires a macOS-26 self-hosted runner. See sc-2352.
    runs-on: macos-15
    env:
      # Override the .cargo/config.toml default (26.2); non-forced there, so this env wins on the
      # hosted SDK-15 runner. See .cargo/config.toml for the authoritative 26.2 rationale — do NOT
      # "reconcile" this to a lower target there (sc-2772: the NAX 16-bit kernels miscompile to
      # garbage below 26.2).
      MACOSX_DEPLOYMENT_TARGET: "15.0"
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Toolchain versions
        run: |
          sw_vers
          xcode-select -p
          xcodebuild -version
          cmake --version

      - name: Ensure Metal compiler
        # mlx-sys builds MLX's Metal kernels from source via cmake, which needs the
        # `metal` compiler. Xcode 16+ ships it as a separately-downloadable component;
        # Xcode 15 has it built in. Download only if missing so this works on either.
        run: |
          if ! xcrun --find metal >/dev/null 2>&1; then
            echo "metal not found — downloading the Metal Toolchain component"
            sudo xcodebuild -downloadComponent MetalToolchain
          fi
          echo "metal: $(xcrun --find metal)"
          xcrun metal --version || true

      - name: Install Rust (pinned by rust-toolchain.toml)
        # No `@stable` — that would install whatever stable is current and ignore the pin. `rustup
        # show` installs the channel named in rust-toolchain.toml (its `components` come with it);
        # the explicit add is idempotent belt-and-suspenders. Bump the toolchain by editing the toml.
        run: |
          rustup show
          rustup component add rustfmt clippy
          rustc --version

      - name: Cache cargo + mlx-sys build
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --locked --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --locked --workspace
